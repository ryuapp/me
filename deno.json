{
  "tasks": {
    "miru": "cd zig-out; ./miru.exe",
    "prepare": "mkdir zig-out",
    "build": "deno task build:windows",
    "build:windows": "cd zig-out && zig build-exe ../src/main.zig -O ReleaseSmall -target x86_64-windows-msvc --name miru"
  }
}
